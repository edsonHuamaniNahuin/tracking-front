
export const ITEMS_PER_PAGE_OPTIONS = [5, 10, 15, 25, 50, 100];

/**
 * Número por defecto de filas en modo loading,
 * en caso de que no haya meta.per_page disponible.
 */
export const DEFAULT_LOADING_ROWS = 5;

